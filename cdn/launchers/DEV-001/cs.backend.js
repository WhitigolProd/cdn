as1 = `
<div class="item">
    <img class="header" src="${sz00ervers.s1.background}"/>
    <h1 class="title">${sz00ervers.s1.name}</h1>
    <div class="desc">${sz00ervers.s1.description}</div>
    <div class="btn cs-launch-1">Connect <span>&rarr;</span></div>
</div>`;

as2 = `
<div class="item">
    <img class="header" src="${sz00ervers.s2.background}"/>
    <h1 class="title">${sz00ervers.s2.name}</h1>
    <div class="desc">${sz00ervers.s2.description}</div>
    <div class="btn cs-launch-2">Connect <span>&rarr;</span></div>
</div>`;

as3 = `
<div class="item">
    <img class="header" src="${sz00ervers.s3.background}"/>
    <h1 class="title">${sz00ervers.s3.name}</h1>
    <div class="desc">${sz00ervers.s3.description}</div>
    <div class="btn cs-launch-3">Connect <span>&rarr;</span></div>
</div>`;

as4 = `
<div class="item">
    <img class="header" src="${sz00ervers.s4.background}"/>
    <h1 class="title">${sz00ervers.s4.name}</h1>
    <div class="desc">${sz00ervers.s4.description}</div>
    <div class="btn cs-launch-4">Connect <span>&rarr;</span></div>
</div>`;

as5 = `
<div class="item">
    <img class="header" src="${sz00ervers.s5.background}"/>
    <h1 class="title">${sz00ervers.s5.name}</h1>
    <div class="desc">${sz00ervers.s5.description}</div>
    <div class="btn cs-launch-5">Connect <span>&rarr;</span></div>
</div>`;

as6 = `
<div class="item">
    <img class="header" src="${sz00ervers.s6.background}"/>
    <h1 class="title">${sz00ervers.s6.name}</h1>
    <div class="desc">${sz00ervers.s6.description}</div>
    <div class="btn cs-launch-6">Connect <span>&rarr;</span></div>
</div>`;


// Set Titlebar Title
$(document).ready(()=>{
    $('.cs-title').text(community.name + ' Launcher')
})
